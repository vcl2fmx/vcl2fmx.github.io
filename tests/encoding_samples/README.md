# Encoding Regression Samples

`UTF8_LanguageComments.pas` is a UTF-8 source file with comments in several languages.

Use it to verify that conversion preserves non-English comments and does not introduce mojibake when the source is UTF-8.
