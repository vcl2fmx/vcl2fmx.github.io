unit ContractUnsupportedMessageTryExceptDepth;
interface
uses FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Types, System.Classes, System.SysUtils,
  System.Variants;
type
  TContractUnsupportedMessageTryExceptDepth = class(TForm)
  protected
  // FMX manual review: procedure WMSysCommand(var Msg: TWMSysCommand); message WM_SYSCOMMAND;
  end;
implementation
  // FMX manual review: procedure TContractUnsupportedMessageTryExceptDepth.WMSysCommand(var Msg: TWMSysCommand);
  // FMX manual review: begin
  // FMX manual review: try
  // FMX manual review: if Msg.CmdType = SC_MINIMIZE then
  // FMX manual review: Exit;
  // FMX manual review: except
  // FMX manual review: on E: Exception do
  // FMX manual review: raise;
  // FMX manual review: end;
  // FMX manual review: inherited;
  // FMX manual review: end;
  // FMX manual review: 
end.
