unit ContractSystemCommandHandlerCloseOnly;
interface
uses FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Types, System.Classes, System.SysUtils,
  System.Variants;
type
  TContractSystemCommandHandlerCloseOnly = class(TForm)
  private
  // FMX manual review: procedure WMSysCommand(var Msg: TWMSysCommand); message WM_SYSCOMMAND;
  end;
implementation
  // FMX manual review: procedure TContractSystemCommandHandlerCloseOnly.WMSysCommand(var Msg: TWMSysCommand);
  // FMX manual review: begin
  // FMX manual review: if (Msg.CmdType and $FFF0) = SC_CLOSE then
  // FMX manual review: Close
  // FMX manual review: else
  // FMX manual review: inherited;
  // FMX manual review: end;
  // FMX manual review: 
end.
